My Javascript:

window.addEventListener('DOMContentLoaded', function() {
    var overlay = document.getElementById('overlay');
    setTimeout(function() {
      // fade out
      overlay.style.opacity = 0;
      // remove after transition
      setTimeout(function(){ overlay.style.display = "none"; }, 500); // 500ms is the transition time
    }, 4000); // 4000ms = 4 seconds
});


My html:

<div id="overlay">
      <h1>Making things look nice<span class="blinking">.</span><span class="blinking" style="animation-delay: 0.2s;">.</span><span class="blinking" style="animation-delay: 0.4s;">.</span></h1>
</div>

My CSS:

@keyframes slideIn {
    to {
        transform: skewY(20deg) translate3d(0, 0, 0);
    }
}

@keyframes slideInContent {
    to {
        transform: skewY(-20deg) rotateZ(20deg) translate3d(0, 0, 0);
    }
}

@keyframes slideInFromLeft {
    0% {
        transform: translateX(-100%);
        opacity: 0;
    }

    100% {
        transform: translateX(0);
        opacity: 1;
    }
}

/* Desktop */
@media only screen and (min-width: 1280px) {
        .diagonal-column1 {
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        width: 100%;
        height: 100%;
        /* margin-bottom: 10rem; */
        transform: skewY(20deg) translate3d(-100%, -100%, 0);
        animation: slideIn 2s forwards;
        animation-delay: 4s;
        background-color: gray;
        overflow: hidden;
    }
}

/* Tablet Landscape */
@media only screen and (min-width: 768px) and (max-width: 1023px) {
    .center {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    width: 100%;
    height: 100%;
    background-color: orange;
    overflow: hidden;
    animation: slideInFromLeft 2s forwards;
    animation-delay: 4s;
    }
}

This is just a code snippet. What I'm trying to do is have this overlay for 4 seconds, have a 4 second delay on the animations to wait for the overlay to disappear. The problem I am having is that when I resize my screen the animation-delay is still 4 seconds, but I only need the animation-delay for the overlay. How can I fix this? And can you edit my provided code and comment what was changed or added?